{
  "name": "hospital-appointment-booking",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm run install:all && npm run db:up",
    "install:all": "npm install --prefix api && npm install --prefix web",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d --wait",
    "db:psql": "docker compose exec -it db psql -U postgres -d booking",
    "api:dev": "npm run start:dev --prefix api",
    "web:dev": "npm run dev --prefix web",
    "typecheck": "npm run typecheck --prefix api && npm run typecheck --prefix web"
  }
}